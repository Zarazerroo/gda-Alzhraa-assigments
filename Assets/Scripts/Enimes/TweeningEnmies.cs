using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;
using UnityEditor.VersionControl;

public class TweeningEnmies : MonoBehaviour
{
    // Start is called before the first frame update
    void OnEnable()
    {
        // transform.DOMove(new Vector3(150, 4, 183), 5).SetEase(Ease.OutCubic);
    }

}
